"""Internal helpers for the Urbanomy agent interface.

Structure:
- ``common``: shared contracts and LangChain/LangGraph helpers
- ``block_parameters``: parsing, formatting, and metadata for block info requests
- ``district_optimization``: deterministic parsing/formatting for optimization flows
- ``general_qa`` and ``visualization``: domain metadata and future helpers

Flat modules in this package are kept only as compatibility wrappers.
"""
